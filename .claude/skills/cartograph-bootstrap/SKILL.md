---
name: cartograph-bootstrap
description: Legacy compatibility name for the unified start workflow. Use only when a user invokes cartograph-bootstrap; prefer /start for new and resumed project work.
---

# Cartograph Bootstrap

Read `../start/SKILL.md` and follow that workflow. Tell the user the shorter
command is `/start`. Do not call separate principles, pack, protocol, handoff,
and injection commands when one privacy-aware start packet is sufficient.
