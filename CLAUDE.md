# Cartograph

Read `AGENTS.md`, `docs/PROJECT_PACKET.md`, and the active file in `docs/plans/`.
Use `/valtor <plan>` for orchestrated work. The graph is read-only observation,
gray is the default, and every non-gray status must cite digest-bound evidence.
